var target : Transform;

function Start () {
	transform.position.x = target.position.x;
	transform.position.z = target.position.z;	
}

function Update () {
	transform.position.x = target.position.x;
	transform.position.z = target.position.z;
}